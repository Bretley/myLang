type Cartesian =  {
    Int x y;
}

type Polar = {
    Int r theta;
}

f = Int a => {
    d = 1;
    f = (b < 3);
    (d+5 > f)
}


comparator = Int a b => {
    d = 0;
    d = [ (a < b) => {
            d = 3; 
            while ( d < c) {
                d = d + d;
            }
            d
        }
        [ (a > b) => a[0];
        [ non => 0;;
    a
}

fact = Int a => {
    [ (a > b) => a(b);
    [ non => 1;
}

