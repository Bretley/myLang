ack = Int m n => {
    b = 5;
    b = (m > n);
    b
}
/*
comparator = Int a b =>
    [(a > b) => 1;
    [(a < b) => -1;
    [ non => 0;

/*
fact = Int a =>
    [(a > 1) => a * fact(a-1);
    [ non => 1;


/*
fact2 = Int a => {
    Int sum;
    sum = 1;
    while (a>1) {
        sum = sum * a;
    }
    sum
}
*/

